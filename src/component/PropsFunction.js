import React from 'react';

function PropsFunction(Props) {
    return (
        <div>
            <h3>Name: {Props.name} Age: {Props.age} Distruct: {Props.distruct}</h3>
        </div>
    );
}
export default PropsFunction;